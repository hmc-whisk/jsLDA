numTopics // in processvars (used by sweep)
vocabularySize // in processvars (used by sweep)
// Topic model parameters
var documentTopicSmoothing = 0.1; // (used by sweep)
var topicWordSmoothing = 0.01; // (used by sweep)
tokensPerTopic // in processvars (used by sweep)
documents // in processvars (used by sweep)
topicWeights // in processvars (used by sweep)
completeSweeps // in processvars (used by sweep)
requestedSweeps  // in processvars (used by sweep)
var timer; // used in sweep